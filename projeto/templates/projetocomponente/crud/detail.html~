{% extends "base/base.html" %}

{% load bootstrap3 %}

{% block title %} <title>Projeto Complementar - {{ projetocomplementar.nome }}</title> {% endblock title %}

{% block content %}

    <h2>{{ projetocomplementar }}</h2>
    <hr/>
    <p><b>Número:</b> {{ projetocomplementar.numero }}</p>
    <p><b>Responsável: </b> {{ projetocomplementar.responsavel }}</p>
    
    <p> <b>Projeto associado: </b>
         <a href="{% url 'detail_projeto' projetocomplementar.projeto.id %}">{{ projetocomplementar.projeto }}</a>
    </p>
    

    <p><b>Cadastrado:</b> {{ projetocomplementar.data_cadastro }}</p>
    <p><b>Atualizado:</b> {{ projetocomplementar.data_atualizado }}</p>

    <hr/>
    {% if request.user == projetocomplementar.criado_por or request.user.is_superuser %}
      <p>
        <a href="{{ projetocomplementar.get_projetocomplementar_update_url }}">Editar</a> | 
        <a href="{{ projetocomplementar.get_projetocomplementar_delete_url }}">Apagar</a> |
        <a href="{% url 'list_projetocomplementar_projeto' %}">Voltar</a>
      </p>
    {% endif %}
{% endblock content %}
