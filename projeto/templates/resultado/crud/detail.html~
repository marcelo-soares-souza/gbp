{% extends "base/base.html" %}

{% load bootstrap3 %}

{% block title %} <title>Projetos - Resultado de Projeto {{ resultado.numero }}</title> {% endblock title %}

{% block content %}

    <h2>{{ object }}</h2>

    <hr/>

    <p>
      <b>Projeto Associado:</b>
      <a href="{% url 'detail_projeto' objetivo.projeto.id %}">{{ objetivo.projeto }}</a>
    </p>

    
    {% if resultado.descricao %}
      <p><b>Descrição:</b> {{ resultado.descricao }}</p>
    {% endif %}
    

    <p>
      <b>Objetivo Associado:</b>
      {% for objetivo in resultado.objetivo.all %}
        <a href="{% url 'detail_objetivo_projeto' objetivo.id %}">{{ objetivo.numero }}</a>
      {% endfor %}
    </p>
      
    <p><b>Cadastrado:</b> {{ resultado.data_cadastro }}</p>
    
    <hr/>

    {% if request.user == resultado.criado_por or request.user.is_superuser %}
      <p>
        <a href="{{ resultado.get_resultado_update_url }}">Editar</a> | 
        <a href="{{ resultado.get_resultado_delete_url }}">Apagar</a> |
        <a href="{% url 'list_resultado_projeto' %}">Voltar</a>
      </p>
    {% endif %}
{% endblock content %}
